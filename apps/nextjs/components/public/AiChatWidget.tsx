"use client";
/**
 * Widget chat cua landing page.
 *
 * Ke tu ban tich hop "Phuong an B", source that su nam o packages/chat-widget
 * (dung chung voi app Vite/CRM, 1 build pipeline duy nhat). File nay chi con
 * la re-export mong de khong vo cac import cu (AiChatWidgetWrapper.tsx).
 */
export { AiChatWidget, default } from "@sgs/chat-widget";
