import PublicProjectComposer from "@/components/content/PublicProjectComposer";
export const dynamic = "force-dynamic";
export default async function EditPublicProjectPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <main className="mx-auto max-w-3xl px-4 py-10"><h1 className="mb-8 text-3xl font-bold">Sửa dự án giới thiệu</h1><PublicProjectComposer id={id} /></main>; }