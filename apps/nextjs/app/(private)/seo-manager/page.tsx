"use client";

export default function SeoManagerPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold mb-6" style={{ color: "var(--text-primary)" }}>
        SeoManager
      </h1>
      {/* CSR — component migrated from pages/SeoManager.tsx */}
      <p style={{ color: "var(--text-secondary)" }}>Loading SeoManager...</p>
    </div>
  );
}
